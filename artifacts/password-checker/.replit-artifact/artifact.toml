kind = "web"
previewPath = "/"
title = "Password Strength Checker"
version = "1.0.0"
id = "artifacts/password-checker"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19334

[services.development]
run = "pnpm --filter @workspace/password-checker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/password-checker", "run", "build" ]
publicDir = "artifacts/password-checker/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19334"
BASE_PATH = "/"
